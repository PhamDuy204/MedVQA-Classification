[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "medvqa-project"
version = "0.1.0"
description = "Medical VQA"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
